{{- if .Values.tlsInternal.enabled }}
{{ include "governor.cert-manager.certificate" (dict
  "root" .
  "name" (printf "%s-cert" .Values.front.name)
  "app" .Values.front.name
  "secretName" (printf "%s-tls" .Values.front.name)
  "cert" .Values.tlsInternal.certificates
  "certificate" .Values.front.tlsInternal.certificate
  "defaultDnsName" (printf "%s.%s.svc.cluster.local" .Values.front.name .Release.Namespace)
) }}
{{- end }}
