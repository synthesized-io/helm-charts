{{- if .Values.agent }}

============================================================
DEPRECATION WARNING — legacy `agent:` block detected
============================================================
The `agent:` values block is DEPRECATED. It has been renamed
to `worker:` and will be removed in a future release.

Your `agent:` values are still applied — they are merged on
top of the `worker:` defaults, so your existing overrides
continue to work.

IMPORTANT: if you set BOTH `agent.X` and `worker.X` for the
same field, the LEGACY `agent.X` value wins. To avoid
surprises, DO NOT mix the two blocks — pick one.

To migrate, rename `agent:` to `worker:` in your values file
(structure is identical) and remove the `agent:` block:

  agent:                    ->   worker:
    image:                         image:
      repository: my/image           repository: my/image
    container:                     container:
      config:                        config:
        AGENT_TAGS: prod               WORKER_TAGS: prod

At runtime, the worker container still accepts `AGENT_*`
environment variables and remaps them to `WORKER_*`
automatically — but using `WORKER_*` in your values file is
recommended for clarity.

NOTE: if you previously relied on the default K8s resource
name `governor-agent` (e.g. for monitoring, NetworkPolicies,
ServiceMonitors, or external scripts), set the following to
keep the old name:

  agent:
    name: governor-agent

Without this override, your deployment will be renamed to
`governor-worker` on upgrade (a pre-upgrade hook handles the
cleanup of the old resources automatically).

PVC MIGRATION — if you had `agent.persistence.enabled: true`,
the pre-upgrade hook has protected the old `agent-pvc` from
deletion, but the renamed worker now mounts a NEW EMPTY
`worker-pvc` by default.

To reconnect the worker to your existing data, add:

  worker:
    persistence:
      claimName: agent-pvc

(or whatever `agent.persistence.claimName` was set to).
The old `agent-pvc` remains as an unmanaged resource until
you explicitly delete it once migration is complete.
============================================================

{{- end }}

{{- if .Values.assistant.enabled }}

============================================================
Assistant (agent-lab) is ENABLED
============================================================
Deployed as Deployment/Service "{{ .Values.assistant.name }}"
(port {{ .Values.assistant.container.port }}).

Make sure BOTH are supplied via assistant.container.secretEnv
(from an external secret) or assistant.container.secretConfig:
  - ANTHROPIC_API_KEY  (required for any LLM operation)
  - DATABASE_URL       (Postgres for chat threads; a logical DB
                        on the shared TDK/Governor Postgres is fine)
Without DATABASE_URL the pod stays NotReady (/ready = 503).

To deploy Governor WITHOUT the Assistant (e.g. for no-LLM /
no-AI clients), set assistant.enabled=false — the default.
This removes all Assistant pods, services, config, and
secrets.
============================================================
{{- end }}
